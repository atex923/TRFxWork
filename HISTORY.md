# HISTORY

本檔是專案歷史區。根目錄只保留最新版號的程式檔；舊版號程式檔集中存放在 `history/old_versions/`，並由 Git commit、tag 與分支保存。

## 歷史區規則

- `README.md`、`AGENTS.md`、主程式常數只顯示目前最新版號。
- 每次改版前，先將上一版建立 Git tag，例如 `v0.1.7`。
- 每次改版都要在主程式 `APP_RELEASE_SUMMARY` 與本檔新增摘要。
- 每次修改程式後，第三碼加 1，例如 `V0.1.7` -> `V0.1.8`。
- 根目錄只保留最新版號檔案，例如 `TR_FxWork_V0_3_3_2.py` 與 `TR_FxWork_V0_3_3_2.pyw`。
- 舊版號程式檔集中存放在 `history/old_versions/`，並可使用 Git tag、commit 與分支查詢。
- 主頁 README 與目前分支永遠維持最新版。
- 第一碼或第二碼進版時，將上一個大版/小版的多筆第三碼摘要濃縮成一段大版摘要，避免歷史區過長。

## V0.3.3.2

- 使用使用者提供的 `TR_FxWork_V0_3_3_2_package.zip` 更新程式。
- 第六分頁可支用額度超額提示改為可支用金額紅字加粗。
- 整理 Git 追蹤中的舊版號程式檔，移入 `history/old_versions/` 歷史區。

## V0.2.5

- 第二分頁前三個表格的日期與名稱欄寬放大 1.5 倍。

## V0.2.4

- 縮窄第二分頁四個表格欄寬，讓晴雨表日期、上午、下午、天氣、場地、備註欄位可一次呈現。

## V0.2.3

- 修正第二分頁右下角統一「資料編輯鎖定解除」勾選在啟動時尚未建立變數造成的啟動錯誤。

## V0.2.2

- 第二分頁四個表格改為同一行呈現。
- 第二分頁四個表格標題字體放大為 14pt。
- 第二分頁資料編輯鎖定解除改為右下角單一勾選，統一管制假期、補班日、鐵路疏運與晴雨表。

## V0.2.1

- 第九分頁「預算資料」在「預算」與「契約」兩區各新增「讀入預算書」，可讀取 `.xlsx` 並顯示資料。
- 新增第十分頁「變更資料」，包含第幾次變更預算、變更歷程、變更金額、變更事由、送預算書時間、讀入預算書、上半部縮頁與下半部變更情形顯示。

## V0.2.0

- 第三分頁施工日曆改為依每日實際工期累計到契約工期，遇晴雨表扣工期時仍會延伸顯示到契約天數。
- 新增第九分頁「預算資料」，分為「預算」與「契約」兩大框線區域，底色分別為粉藍與粉綠。

## V0.1.15

- 第三分頁施工日曆每週左側標題下移一列，對齊右側假日、疏運日、雨天與工作日數資料列。

## V0.1.14

- 第二分頁改為上下兩個區塊，中間加入可拖曳高度的分隔線。
- 上半區改為假期表、補班日表、鐵路疏運表三欄並排；晴雨表維持在下半區。

## V0.1.13

- 修正 `V0.1.12` 在部分 Python/Tk 版本啟動時 `PanedWindow.add` 參數不相容的問題。
- 第二分頁改用相容性更高的三段式格線配置，保留假期/補班、晴雨表與鐵路疏運表，日期欄排序功能不變。

## V0.1.12

- 第二分頁改為假期/補班、晴雨表、鐵路疏運表三個可調整高度的瀏覽區，鐵路疏運表固定在同一分頁內顯示。
- 第二分頁各表格的日期欄標題改為日期大小排序，重複點擊可切換由小到大與由大到小。

## V0.1.11

- 上方工程辦理情形摘要新增 `▲/▼` 按鈕，可收合或展開摘要區。
- 將原第二、第三、第四分頁合併為「假期/晴雨/疏運表」單一分頁，保留假期、補班、晴雨與鐵路疏運原功能。

## V0.1.10

- 預算金額區塊「預算內容」改為「總工程預算」，契約金額區塊「決標內容」改為「總工程費用」。
- 預算與契約兩側的「未完工程」改為自動加總發包工程費金額與發包以外項目。
- 契約金額區的總工程費用、發包工程費、包工費不再顯示複製預算按鈕，僅發包以外項目保留複製功能。

## V0.1.9

- 第一分頁工程基本資料顯示區改名為「工程辦理情形摘要」。
- 工程基本資料新增初驗、驗收、決算、保固結束日、保固備註、履保金比例與保固金比例相關欄位。
- 工程費改成「預算金額」與「契約金額」兩區，加入稅金、總計、比率與決標金額複製預算功能。
- 招標情形改成決標、次數、公告上網日、開標日、決標日 5 欄，決標日自動帶入上方決標日期。

## V0.1.8

- 建立「改版先 tag 舊版」規則。
- 明確規定 README 主頁與目前分支只保留最新版。
- 已建立 `v0.1.7` 作為上一版歷史錨點。

## V0.1.7

- 新增歷史區規則。
- 工作區改為只保留最新版號程式檔，舊版檔案由 Git 歷史保存。
- 主程式新增 `APP_VERSION` 與 `APP_RELEASE_SUMMARY`，讓程式碼內也有當版摘要。

## V0.1 系列摘要

- 建立 tkinter + SQLite 的臺鐵工程紀錄工具，支援多工程、基本資料、假期表、晴雨表、鐵路疏運表、施工日曆、計價、工程執行紀錄與工程大事記。
- 加入自動儲存、資料編輯鎖定、工程密碼、備份/匯入、Excel 匯出匯入、異地備份與刪除工程功能。
- 施工日曆逐步完善假期、補班、鐵路疏運、晴雨表扣工期與不同解析度整月顯示。
- 資料庫主檔名改為 `TRFxWork_db`，保留舊 `TR_FxWork.db` 相容讀取，並避免 DB 進 Git。
- 支援 Nuitka onefile 打包、固定 exe 旁資料庫位置，並加入 icon 與「明確要求時才轉 exe」規則。
